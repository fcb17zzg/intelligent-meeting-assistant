# 前端界面开发完成说明

## 📋 项目概览

前端使用 **Vue 3** + **Vite** 开发，通过 REST API 与 FastAPI 后端集成，提供完整的会议管理、音频上传、转录和分析功能。

## 🎯 主要功能

### 1. **会议管理**
- 📝 创建新会议
- 📋 会议列表展示
- 🔍 搜索和筛选
- ✏️ 编辑会议信息
- 🗑️ 删除会议
- 📊 统计概览（总数、待处理、已完成等）

### 2. **音频上传**
- 🎤 拖拽或点击上传音频
- 📤 支持多种格式（WAV、MP3、M4A、OGG）
- 📊 上传进度显示
- ✅ 文件验证（大小、格式）
- ⚠️ 友好的错误提示

### 3. **会议转录和分析**
- 🔄 自动转录功能
- 📄 摘要生成
- 🎯 关键议题提取
- ⭐ 重点突出标记
- ✅ 行动项管理
- 🎤 发言人统计

### 4. **任务管理**
- ✅ 任务列表显示
- 🔠 优先级标记（高/中/低）
- 📅 截止日期管理
- 👤 任务分配
- ✏️ 编辑任务
- 🗑️ 删除任务
- 📊 任务完成率统计

## 📁 项目结构

```
frontend/
├── public/                    # 静态资源
├── src/
│   ├── api/                   # API 服务
│   │   ├── client.js          # Axios 客户端
│   │   └── index.js           # API 方法集合
│   ├── components/            # 可复用组件
│   │   ├── AudioUploader.vue  # 音频上传组件
│   │   ├── SummaryDisplay.vue # 摘要显示组件
│   │   └── TaskList.vue       # 任务列表组件
│   ├── pages/                 # 页面组件
│   │   ├── MeetingList.vue    # 会议列表页
│   │   ├── MeetingDetail.vue  # 会议详情页
│   │   └── CreateMeeting.vue  # 创建会议页
│   ├── router/                # 路由配置
│   │   └── index.js
│   ├── stores/                # Pinia 状态管理
│   │   ├── meetingStore.js    # 会议店铺
│   │   └── taskStore.js       # 任务店铺
│   ├── styles/                # 全局样式
│   │   └── main.scss
│   ├── utils/                 # 工具函数
│   │   └── dateUtils.js       # 日期处理
│   ├── App.vue                # 根组件
│   └── main.js                # 入口文件
├── index.html                 # HTML 入口
├── package.json               # 依赖配置
├── vite.config.js             # Vite 配置
└── .gitignore
```

## 🚀 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
# 或使用 yarn/pnpm
yarn install
pnpm install
```

### 2. 开发模式运行

```bash
npm run dev
```

应用将在 `http://localhost:3000` 启动

### 3. 生产构建

```bash
npm run build
```

构建输出在 `dist/` 目录

### 4. 预览构建结果

```bash
npm run preview
```

## 🔧 配置

### API 服务器地址

编辑 `vite.config.js` 中的 proxy 配置：

```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8000',  // 修改为实际后端地址
      changeOrigin: true,
    },
  },
}
```

或者在 `.env` 文件中设置：

```
VITE_API_URL=http://localhost:8000/api
```

### 环境变量

创建 `.env.local` 文件：

```
VITE_API_URL=http://localhost:8000/api
VITE_APP_TITLE=智能会议助手
```

## 📦 依赖说明

### 核心框架
- **vue**: 3.3.4 - 前端框架
- **vue-router**: 4.2.4 - 路由管理
- **pinia**: 2.1.4 - 状态管理

### UI 库
- **element-plus**: 2.4.2 - UI 组件库
- **@element-plus/icons-vue**: 2.1.0 - 图标库

### 工具
- **axios**: 1.6.0 - HTTP 客户端
- **date-fns**: 2.30.0 - 日期处理

### 开发工具
- **vite**: 4.4.9 - 构建工具
- **@vitejs/plugin-vue**: 4.3.4 - Vue 支持
- **sass/scss**: 样式预处理器

## 🎨 UI 特点

- ✨ 现代化设计
- 🎭 渐变色背景和过度动画
- 📱 完全响应式布局
- ♿ 无障碍访问支持
- 🌙 暗色模式就绪（可扩展）

## 🔐 API 集成

所有 API 调用都通过统一的 axios 客户端，支持：
- 请求/响应拦截
- 自动错误处理
- Token 认证（预留）
- 请求超时设置

## 🧪 测试数据

在开发环境中，可以使用以下测试数据：

### 创建会议示例

```javascript
{
  title: "Q4 产品规划会议",
  description: "讨论第四季度产品方向和任务分配",
  duration: 120,
  participants: 8,
  location: "会议室 A",
  organizer: "张三"
}
```

## 📝 主要组件说明

### AudioUploader 组件

音频上传组件，支持拖拽和点击上传。

```vue
<AudioUploader 
  :meeting-id="meetingId"
  @upload-success="handleSuccess"
  @upload-error="handleError"
/>
```

**Props:**
- `meetingId` (String/Number, required): 会议 ID

**Events:**
- `upload-success`: 上传成功
- `upload-error`: 上传失败
- `file-selected`: 文件已选择

### SummaryDisplay 组件

摘要显示组件，展示会议摘要、任务等信息。

```vue
<SummaryDisplay
  :summary="summaryData"
  :loading="isLoading"
  @refresh="loadSummary"
  @update-notes="updateNotes"
/>
```

**Props:**
- `summary` (Object): 摘要数据
- `loading` (Boolean): 加载状态

**Events:**
- `refresh`: 刷新摘要
- `update-notes`: 更新笔记
- `update-action-item`: 更新行动项

### TaskList 组件

任务列表组件，支持筛选、排序和编辑。

```vue
<TaskList
  :tasks="taskArray"
  @complete-task="handleComplete"
  @update-task="handleUpdate"
  @delete-task="handleDelete"
/>
```

**Props:**
- `tasks` (Array): 任务数组

**Events:**
- `complete-task`: 标记完成
- `update-task`: 更新任务
- `delete-task`: 删除任务

## 🐛 常见问题

### 1. CORS 错误

确保后端 CORS 配置正确。在 `app.py` 中检查：

```python
origins = [
    "http://localhost:3000",  # 前端开发地址
    ...
]
```

### 2. API 连接失败

检查：
1. 后端服务是否运行（`python -m uvicorn app:app --reload`）
2. API 地址是否正确
3. 网络连接是否正常

### 3. 样式显示异常

清空浏览器缓存：
```bash
npm run build  # 重新构建
npm run dev    # 重新运行
```

## 📚 相关文档

- [Vite 官方文档](https://vitejs.dev/)
- [Vue 3 文档](https://vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Pinia 文档](https://pinia.vuejs.org/)

## 🤝 贡献

欢迎提交问题和改进建议！

## 📄 许可证

MIT License

---

**最后更新**: 2024年2月
**版本**: 1.0.0
